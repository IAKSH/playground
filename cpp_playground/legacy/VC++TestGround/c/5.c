/*
#include <stdio.h>
#include <stdbool.h>

bool isPrime(int n)
{
	for (int i = 2; i < n; i++)
	{
		if ((n % i) == 0)
			return false;
	}
	return true;
}


int main(void)
{
	int n;
	scanf("%d", &n);
	if (isPrime(n))
		puts("是素数");
	else
		puts("不是素数");
	return 0;
}
*/