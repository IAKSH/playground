// Pipeline,RenderPass,Frame,Shader,GPUBuffer
// (FBO,Shader program,UBO,SSBO)

#include <glad/gles2.h>

namespace nioes {
    // OpenGL (ES) FBO wrapper
    class Frame {

    };

    // 绘制阶段，虚类
    class Pass {

    };

    // 管线，用于组织Pass
    class Pipeline {

    };
}